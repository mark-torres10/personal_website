# TO BE DELETED ONCE THE SKILL IS DONE, but NOT YET

- 1-2 sentences
- Avoid excessive bolding.
- Executive-summary style update.
- Lead with the system or user.
- Focus on outcome, less on logistics.
- Always add the PR link, like "[PR #](PR link)"
- ...
- Avoid excessive implementation detail. Focus on describing the problem solved and the key intervention. Only reference libraries or tool names if critical to the change.
- Cut implementation rationale and digests. Focus on what was actually implemented.
- Avoid references to versioning (e.g., "V1 of graph").
- Avoid "what this completes / what it doesn’t do yet" narration. Focus on what is explicitly implemented.

Consider that each PR will have verbose detail on implementation, rationale, and choices made, all as part of its PR description. Therefore, the CHANGELOG should be a terse, executive-style summary of the history of the PRs in the repo. The CHANGELOG should be easily readable as a list of the core features and work done on each PR, while avoiding delving too much into implementation detail and instead focusing on the end result.
